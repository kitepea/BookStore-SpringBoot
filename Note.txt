- Consumer Layer hay Controller: là tầng giao tiếp với bên ngoài và handler các request từ bên ngoài tới hệ thống.

- Service Layer: Thực hiện các nghiệp vụ và xử lý logic

- Repository Layer:: Chịu trách nhiệm giao tiếp với các DB, thiết bị lưu trữ, xử lý query và trả về các kiểu dữ liệu mà tầng Service yêu cầu.

- @PathVariable("id"): Đây là một annotation của Spring MVC dùng để đánh dấu rằng tham số id sẽ được lấy từ một phần của URL. Ví dụ, nếu bạn có một yêu cầu tới URL /mylist/123, thì id sẽ được gán giá trị là 123

- @GetMapping("/book_register") meaning if there are get request to this /book_register
i.e: <a class="nav-link text-white" href="/book_register">New Books Register</a>

      public String bookRegister() {
          return "bookRegister"; meaning href to bookRegister.html
      }

- @PostMapping("/save")
      public String addBook(@ModelAttribute Book b)

    @ModelAttribute đánh dấu đối tượng Book được gửi lên bởi Form Request

- @GetMapping("/my_books")
      public String getMyBooks(Model model)


- Source: https://www.youtube.com/watch?v=xnur2ujza6k&ab_channel=ImaginaryEngineering
